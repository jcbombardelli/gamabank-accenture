const chai = require('chai');
const assert = chai.assert;

const { sendMessage } = require('../../src/helpers/nodemailer');

describe('Validar se email esta sendo enviado corretamente', async () => {

    /*it('Deve retornar true caso email foi enviado com sucesso', async () => {
        const email = await sendMessage('bruno_agst@hotmail.com', 'Transferencia realizada com sucesso');
        assert.equal(email, true);
    });*/
});