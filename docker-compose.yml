version: '3'

services:
    database:
        build: ./mysql_data
        environment:
            MYSQL_ROOT_PASSWORD: 'root'
            MYSQL_DATABASE: 'gamabank'
        ports:
            - '3306:3306'
        volumes:
            - ./mysql_volumes:/var/lib/mysql
        restart: always

    adminer:
        image: adminer
        ports:
            - 8080:8080

    # app:
    #   build: .
    #   command: npm run start:dev
    #   depends_on:
    #     - database
    #   ports:
    #     - "3000:3000"
    #   volumes:
    #     - .:/opt/app
